# طريقة الدخول للوحة تحكم Squadly (خطوة بخطوة)

المشكلة اللي كانت موجودة: مفيش أي أدمن Bootstrap تلقائيًا — لازم "أول أدمن" يتعمله يدويًا مرة واحدة بس. اتبع الخطوات دي بالترتيب ومش هتحتاج تكررها تاني.

---

## 1) فعّل تسجيل الدخول بالإيميل في Firebase
1. روح [console.firebase.google.com](https://console.firebase.google.com) → افتح مشروع `squadly-bb782`.
2. من القائمة الجانبية: **Authentication** → **Sign-in method** → فعّل **Email/Password**.

## 2) حمّل مفتاح الخدمة (Service Account Key)
1. من نفس الكونسول: ⚙️ **Project settings** → تبويب **Service accounts**.
2. اضغط **Generate new private key** → هيتنزلك ملف JSON.
3. غيّر اسمه لـ `serviceAccountKey.json` وحطه **في نفس المجلد** اللي فيه ملف `set-admin.js`.

⚠️ **مهم:** الملف ده فيه صلاحيات كاملة على مشروعك — متحطهوش على GitHub أبدًا. أضِف سطر `serviceAccountKey.json` في ملف `.gitignore`.

## 3) شغّل السكربت مرة واحدة من جهازك
افتح Terminal / CMD في نفس المجلد واكتب:

```bash
npm install firebase-admin
node set-admin.js
```

السكربت هيعمل تلقائيًا:
- ينشئ حساب `matrix2006@gmail.com` بكلمة المرور `01224815487` (لو الحساب مش موجود)
- يمنحه صلاحية **admin** الحقيقية (Custom Claim)
- يزامن بياناته في Firestore

لو شفت رسالة `🎉 تم!` في الآخر، يبقى الحساب جاهز.

## 4) ادخل على لوحة التحكم من الموقع
1. افتح `index.html`.
2. انزل لآخر الفوتر، هتلاقي رقم الإصدار `v1.0.4`.
3. اضغط عليه **5 مرات متتالية بسرعة** (خلال ثانية ونص تقريبًا).
4. هتظهر نافذة سرية → سجّل دخول بـ:
   - الإيميل: `matrix2006@gmail.com`
   - الباسورد: `01224815487`
5. هيتم تحويلك تلقائيًا لصفحة `admin.html`.

---

### لو حصلت مشكلة
- **"غير مصرح لك بالوصول"** → السكربت في خطوة 3 ملغيش صح، أو الإيميل اللي دخلت بيه في المودال مش نفس `matrix2006@gmail.com`.
- **النافذة السرية مش بتظهر** → تأكد إنك بتضغط 5 مرات **متتالية بسرعة** على نص رقم الإصدار بالظبط، مش على أي مكان تاني في الفوتر.
- **رسالة "محاولات كثيرة جدًا"** → في حماية Rate limiting مؤقتة، استنى دقيقة وحاول تاني.
- عايز تضيف أدمن تاني بعدين؟ غيّر `ADMIN_EMAIL` و`ADMIN_PASSWORD` في `set-admin.js` وشغّله تاني.
